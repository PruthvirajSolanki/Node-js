<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Book</title>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(135deg, #74b9ff, #ffeaa7);
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .form-container {
            background-color: #ffffff;
            padding: 40px;
            border-radius: 15px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
            width: 80%;
            max-width: 600px;
        }

        h2 {
            color: #0984e3;
            text-align: center;
            margin-bottom: 30px;
        }

        .btn-primary {
            border: none;
        }

        .form-select:focus {
            border-color: #74b9ff;
            box-shadow: 0 0 5px #74b9ff;
        }
    </style>
</head>

<body>

    <div class="form-container">
        <h2>Add Book</h2>
        <form method="post" action="/insertBook">

            <div class="mb-3">
                <label for="book_name" class="form-label">Book_Name</label>
                <input type="text" class="form-control" id="book_name" name="book_name" required>
            </div>

            <div class="mb-3">
                <label for="book_author" class="form-label">Book_Author</label>
                <input type="text" class="form-control" id="book_author" name="book_author" required>
            </div>

            <div class="mb-3">
                <label for="book_price" class="form-label">Book_Price (₹)</label>
                <input type="number" class="form-control" id="book_price" name="book_price" required>
            </div>

            <div class="mb-3">
                <label for="book_pages" class="form-label">Book_Pages</label>
                <select class="form-select" id="book_pages" name="book_pages" required>
                    <option selected disabled>--- Select Page Range ---</option>
                    <option value="50-100">50 - 100 Pages</option>
                    <option value="101-200">101 - 200 Pages</option>
                    <option value="201-300">201 - 300 Pages</option>
                    <option value="301-500">301 - 500 Pages</option>
                    <option value="500+">500+ Pages</option>
                </select>
            </div>

            <button type="submit" class="btn btn-primary w-100">Add Book</button>
        </form>

        <div class="text-center mt-3">
            <a href="/" class="btn btn-secondary w-100">View All Books</a>
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>